apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blockbook

resources:
  - pvc.yaml
  - service.yaml
  - deployment.yaml
  - ingress.yaml

images:
  - name: registry.liv.io/liv/blockbook
    newTag: 0.6.0-1
